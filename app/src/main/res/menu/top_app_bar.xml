<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/settings"
        android:icon="@drawable/round_settings"
        android:title="@string/menu_settings_title"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/favorites"
        android:icon="@drawable/round_favorite"
        android:title="@string/menu_favorites_title"
        app:showAsAction="withText" />

    <item
        android:id="@+id/watch_later"
        android:icon="@drawable/round_watch_later"
        android:title="@string/menu_watch_later_title"
        app:showAsAction="withText" />

    <item
        android:id="@+id/collection"
        android:icon="@drawable/round_video_library"
        android:title="@string/menu_collection_title"
        app:showAsAction="withText" />
</menu>